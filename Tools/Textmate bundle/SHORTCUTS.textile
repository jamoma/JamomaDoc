j + tab -> Jamoma 

Max shortcuts:
jc + tab -> "(jamobj)jcom.{var}":components/{var}.html 
jm + tab -> "(jamobj)jmod.{var}":modules/{var}.html 

Team:
tap + tab -> "Timothy Place":http://www.74objects.com/  with or without url
same for tl, to, pb, np, jln, arj

Site:
www + tab -> "{var(page name)}":http://jamoma.org/{var(page name)}.html page 

Picture:
lightbox + tab -> !(lightbox)http://site.com/image(Title)!
thumb + tab -> !(lightbox-thb)http://site.com/image(Title)!

img + tab -> !http://site.com/image(Title)!

Other:

info + tab -> p(info). $1

Note that bc. can be use instead, since, the css is the same so far.

++ + tab -> p(seealso). $1

To be used (most likely) at the bottom of pages to specify links to other tutorial pages.

tbl + tab ->

|_.Header item |_.Header 2d item |_.Header 3d item |
|  |  |  |

Note that you can tab multiple times to jump from one field to another.

i + tab -> integer
d + tab -> decimal
a + tab -> array
s + tab -> string
b + tab -> boolean
? + tab -> generic

